<%= form_with(model: chessboard, local: true) do |form| %>
  <% if chessboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chessboard.errors.count, "error") %> prohibited this chessboard from being saved:</h2>

      <ul>
      <% chessboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dimension %>
    <%= form.number_field :dimension %>
  </div>

  <div class="field">
    <%= form.label :theme %>
    <%= form.text_field :theme %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
